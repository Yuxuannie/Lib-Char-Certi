"""Relocated legacy compute engines (runtime canonical copy).

These scripts are invoked as subprocesses by the stage wrappers in
``cert_data_process/stages/``. They originate from the repo's
``archive/2-data_process/`` reference tree; the copies here make the package
self-contained for delivery. Edit these (not the archived originals) for runtime
behavior.
"""
